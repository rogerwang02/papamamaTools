/* pages/toolbox/index.wxss */
page {
  background-color: #F7F8FA;
}

.container {
  padding: 30rpx;
  min-height: 100vh;
  background-color: #F7F8FA;
}

.header {
  margin-bottom: var(--spacing-lg);
  padding-left: 10rpx;
}

.title {
  font-size: var(--font-xl);
  font-weight: bold;
  color: var(--text-main);
  display: block;
  margin-bottom: 10rpx;
}

.subtitle {
  font-size: var(--font-base);
  color: var(--text-light);
}

/* Grid Layout */
.tool-grid {
  display: grid;
  grid-template-columns: repeat(var(--grid-cols), 1fr);
  gap: var(--spacing-base);
}

.tool-item {
  background: #fff;
  border-radius: 20rpx;
  padding: 30rpx 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: var(--spacing-base);
  box-shadow: 0 4rpx 10rpx rgba(0, 0, 0, 0.03);
  box-sizing: border-box;
}

.tool-item:active {
  transform: scale(0.98);
  opacity: 0.9;
}

.icon-box {
  width: var(--icon-size);
  height: var(--icon-size);
  border-radius: 30rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: var(--spacing-base);
}

.tool-icon {
  font-size: calc(var(--icon-size) * 0.6);
}

.tool-name {
  font-size: var(--font-base);
  color: var(--text-main);
  font-weight: bold;
  line-height: var(--line-height);
}

/* Colors */
.bg-red {
  background: #FFEBEE;
}

.bg-cyan {
  background: #E0F7FA;
}

.bg-blue {
  background: #E3F2FD;
}

.bg-purple {
  background: #F3E5F5;
}

.bg-gray {
  background: #F5F5F5;
}

.bg-indigo {
  background: #E8EAF6;
}

/* 医保凭证提示模态框样式 */
.medical-card-modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.6);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 9999;
}

.medical-card-modal-content {
  width: 85%;
  max-width: 600rpx;
  background-color: #fff;
  border-radius: 24rpx;
  overflow: hidden;
}

.medical-card-modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 40rpx 30rpx 20rpx;
  border-bottom: 2rpx solid #F0F0F0;
}

.medical-card-modal-title {
  font-size: var(--font-lg);
  font-weight: bold;
  color: var(--text-main);
}

.medical-card-modal-close {
  font-size: var(--font-xl);
  color: var(--text-light);
  line-height: 1;
  padding: 0 10rpx;
}

.medical-card-modal-body {
  padding: 40rpx 30rpx;
  text-align: center;
}

.medical-card-modal-tip {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  align-items: center;
  font-size: var(--font-base);
  color: var(--text-sub);
  line-height: var(--line-height);
  gap: 4rpx;
}

.medical-card-modal-tip text {
  line-height: 1.8;
}

.medical-card-modal-tip .highlight-text {
  font-size: var(--font-lg);
  font-weight: bold;
  color: #FF6B00;
  margin: 0 4rpx;
}

.medical-card-modal-footer {
  display: flex;
  border-top: 2rpx solid #F0F0F0;
  padding: 20rpx 30rpx 30rpx;
}

.medical-card-modal-btn {
  flex: 1;
  height: var(--btn-height);
  border-radius: 12rpx;
  font-size: var(--font-lg);
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 10rpx;
  border: none;
}

.medical-card-modal-btn::after {
  border: none;
}

.medical-card-modal-btn.cancel-btn {
  background-color: #F5F5F5;
  color: var(--text-sub);
}

.medical-card-modal-btn.confirm-btn {
  background: linear-gradient(135deg, #FF6B00 0%, #FF8C00 100%);
  color: #fff;
}

/* BMI计算器模态框样式 */
.bmi-modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.6);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 9999;
}

.bmi-modal-content {
  width: 85%;
  max-width: 600rpx;
  background-color: #fff;
  border-radius: 24rpx;
  overflow: hidden;
}

.bmi-modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 40rpx 30rpx 20rpx;
  border-bottom: 2rpx solid #F0F0F0;
}

.bmi-modal-title {
  font-size: var(--font-lg);
  font-weight: bold;
  color: var(--text-main);
}

.bmi-modal-close {
  font-size: var(--font-xl);
  color: var(--text-light);
  line-height: 1;
  padding: 0 10rpx;
}

.bmi-modal-body {
  padding: 40rpx 30rpx;
}

.bmi-input-item {
  margin-bottom: 30rpx;
}

.bmi-input-item:last-child {
  margin-bottom: 0;
}

.bmi-input-label {
  display: block;
  font-size: var(--font-base);
  font-weight: 600;
  color: var(--text-main);
  margin-bottom: 16rpx;
}

.bmi-input {
  width: 100%;
  height: var(--input-height);
  background-color: #F8F8F8;
  border-radius: 12rpx;
  padding: 0 24rpx;
  font-size: var(--input-font-size);
  color: var(--text-main);
  box-sizing: border-box;
}

.bmi-input-placeholder {
  color: #999;
}

.bmi-modal-footer {
  display: flex;
  border-top: 2rpx solid #F0F0F0;
  padding: 20rpx 30rpx 30rpx;
}

.bmi-modal-btn {
  flex: 1;
  height: var(--btn-height);
  border-radius: 12rpx;
  font-size: var(--font-lg);
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 10rpx;
  border: none;
}

.bmi-modal-btn::after {
  border: none;
}

.bmi-modal-btn.cancel-btn {
  background-color: #F5F5F5;
  color: var(--text-sub);
}

.bmi-modal-btn.confirm-btn {
  background: linear-gradient(135deg, #FF6B00 0%, #FF8C00 100%);
  color: #fff;
}

